<template class='doc-view'>
    <header id='fixed-top'>
        格式化颜色
    </header>
    <pre q-code>
        import formatColor from '@hai2007/browser/formatColor.js';
    </pre>
    <pre q-code>
// 把颜色统一转变成rgba(x,x,x,x)格式
// 返回数字数组[r,g,b,a]
var colorArray = formatColor(color);
    </pre>
</template>
